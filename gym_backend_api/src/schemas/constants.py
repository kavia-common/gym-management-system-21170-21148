# PUBLIC_INTERFACE
class WorkoutConstants:
    """Convenience constants for frontend usage if needed."""
    UNITS = ["kg", "lb", "reps", "seconds", "minutes", "meters", "calories"]
    PROGRAM_STATUSES = ["draft", "active", "archived"]
